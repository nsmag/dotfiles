# NSmag's dotfiles 🏠

```bash
./setup.sh

# For mobile development
brew bundle --file=Brewfile-mobile

# Ruby
ruby-install ruby
chruby [ruby-version] # need to restart a shell
gem install bundler
```
